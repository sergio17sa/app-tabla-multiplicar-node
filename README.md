# Notas

Flags consola node

```
Options:

--help     Show help                                             [boolean]
--version  Show version number                                   [boolean]
-b, --base     Es la Base de la tabla de multiplicar             [number] [required]
-l, --listar   Muestra la tabla de multiplicar por consola       [boolean] [required] [default: false]
-h, --high     Muestra la altura de la tabla de multiplicar      [number] [required]

```